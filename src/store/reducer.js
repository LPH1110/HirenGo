const initState = {};

function reducer(state, action) {
    switch (action.type) {
        default:
            throw new Error('Invalid action!');
    }
}

export default reducer;
export { initState };
